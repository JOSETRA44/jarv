// MotdWidget is intentionally thin — the MOTD is now rendered as a
// TerminalBlock.motd entry inside TerminalBlockWidget.
//
// This file provides only a standalone widget for cases where you want
// to show a pre-built motd block (e.g. in tests or a standalone preview).
export 'terminal_block_widget.dart' show TerminalBlockWidget;
